{
  "name": "tripcash",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:rules": "JAVA_HOME=\"$(/usr/libexec/java_home -v 21+ 2>/dev/null || echo /usr/local/opt/openjdk)\" PATH=\"$JAVA_HOME/bin:$PATH\" firebase emulators:exec --only firestore --project tripcash-test \"node --test tests-integration/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1"
  }
}