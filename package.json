{
  "name": "tripcash",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 node --test tests/*.test.mjs",
    "test:rules": "export JAVA_HOME=/usr/local/opt/openjdk; export PATH=\"$JAVA_HOME/bin:$PATH\"; firebase emulators:exec --only firestore --project tripcash-test \"node --test tests-integration/*.test.mjs\"",
    "preflight": "node scripts/preflight.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1"
  },
  "description": "Multi-currency travel converter and shared trip ledger. Offline-first PWA, no build step.",
  "homepage": "https://tripcash.app",
  "repository": {
    "type": "git",
    "url": "https://github.com/archismandinda/tripcash.git"
  }
}
