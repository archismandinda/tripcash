# What is deployed RIGHT NOW — the baseline the next release is measured
# against. `tests-integration/rollout.test.mjs` reads it and nothing else.
#
# This file exists because that test used to read `git HEAD` instead. HEAD
# is not what is deployed; it is whatever was committed last. The moment a
# sprint's own work was committed, the test started comparing the new rules
# and the new client against THEMSELVES: every assertion in it became
# vacuously true, one blew up on a premise about a client that no longer
# existed, and the release gate went red for a reason that had nothing to
# do with the release. A gate that behaves like that gets ignored, and the
# thing it guards is silent permission-denied on somebody's phone.
#
# Both halves are named separately on purpose. They are published by hand,
# in separate acts, and they are routinely NOT the same commit — ADR-0023
# ships the client first and publishes the rules days later, so throughout
# that window the live client is newer than the live rules. One line could
# not say that.
#
# WHO UPDATES THIS, AND WHEN — one line at a time, at the moment the thing
# actually goes live, never in advance:
#   client — after the release is pushed AND devices have had their two
#            opens (§8b.2); this is the commit whose sw.js VERSION they
#            are caching, not merely the newest commit on main.
#   rules  — after firestore.rules is pasted into the Firebase console and
#            published (only the project owner can do this).
#
# Commit shas only. A moving ref — HEAD, a branch name — is precisely the
# defect above, and the parser refuses one.

# TWO CLIENT VALUES, NOT ONE. Split on 10 Aug 2026, because one line was
# being asked to hold two different facts and was quietly wrong about both.
#
#   client-live  — the commit the CDN serves RIGHT NOW. A single knowable
#                  truth, and a machine knows it better than a person: it
#                  drifted four versions behind while nobody noticed,
#                  because a manual update of a machine-knowable fact is a
#                  rule with no check. preflight now reads the live site
#                  directly, so this line is documentation rather than the
#                  thing anything depends on.
#   client-floor — the OLDEST client plausibly still running on somebody's
#                  device. A judgment, not an observation: service workers
#                  update on a device's second open, iOS home-screen installs
#                  hold storage separate from Safari, and neither has a
#                  deadline. Moved deliberately, when we decide to stop
#                  supporting a vintage — never automatically.
#
# rollout.test.mjs measures against the FLOOR. Testing new rules against the
# newest client answers a question nobody has: the phone that breaks is the
# one that has not updated. Until 10 Aug we were getting floor behaviour by
# accident, because the live line had been left stale. Neglect was
# implementing the policy. Now the policy is written down.

client-live  b701b2011bdaa60939ccf28353b7c891bbc8ae19 # v1.78.0 (SW cache v104) — confirmed serving 12 Aug eve; informational, preflight reads the site
client-floor c74d692a02937a3dde66ebaf89274cf0d93e2737   # v1.66.0 (SW cache v91) — deliberate
rules        eb14c6e896cf1861a0219c0f29d969bf623cf83b   # sprint 2's ownership fix — CONFIRMED live in the console 11 Aug
